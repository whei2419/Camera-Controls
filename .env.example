# Mirror APP_URL from the Laravel site’s .env (Herd). Use https if that site uses HTTPS.
VITE_APP_URL=http://Wowsome-micorsite.test

# Optional: multipart field name for POST /api/upload-capture (must match Laravel $request->file('…')).
# VITE_UPLOAD_CAPTURE_FIELD=image
